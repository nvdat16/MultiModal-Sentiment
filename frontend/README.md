# Front-end

Giao diện social feed gọi API FastAPI ở `http://localhost:8000`.

## Chạy frontend

Từ thư mục gốc project:

```bash
cd front-end
python3 -m http.server 5500
```

Mở trình duyệt:

```text
http://localhost:5500/index.html
```
